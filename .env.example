# Shared reference for all environments.

# --- App / Nuxt ---
ENV=development
BASE_API_PATH=/api/v1
# Canonical public origin for SEO
NUXT_PUBLIC_SITE_URL=
APP_PORT=3000

# --- Database ---
# Local Node: loopback. Docker Compose (dev) overrides to mongodb://mongo:27017/...
# Production Compose: set NUXT_MONGO_DB_URI to Atlas in .env.production instead.
MONGODB_URI=mongodb://127.0.0.1:27017/personal-portfolio

# --- Auth (JWT) ---
JWT_SECRET=change-me-to-a-long-random-string
JWT_LIFETIME=1200

# --- Upload storage ---
# local = public/uploads (dev / Docker volume)
# s3    = S3-compatible (Cloudflare R2 / AWS / MinIO)
UPLOAD_DRIVER=local
UPLOAD_PUBLIC_BASE_URL=

S3_ENDPOINT=
S3_REGION=
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=

# --- Dev seed (used by `docker compose --profile seed run --rm seed`) ---
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=ChangeMe1!
SEED_RESET_ADMIN=false
